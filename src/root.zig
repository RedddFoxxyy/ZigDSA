//! By convention, root.zig is the root source file when making a library.

// Dependency/std lib imports:
const std = @import("std");

// Data Structure Imports:
pub const lists = @import("lists.zig");
